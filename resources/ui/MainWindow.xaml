<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <StackPanel Orientation="Horizontal" Background="Transparent" Margin="0,12">
                            <Rectangle x:Name="SelectionBar"
                                       Width="4" Height="40"
                                       Fill="#D4AF37"
                                       Margin="0,0,20,0"
                                       Opacity="0"
                                       RenderTransformOrigin="0.5,0.5">
                                <Rectangle.RenderTransform>
                                    <ScaleTransform ScaleY="0"/>
                                </Rectangle.RenderTransform>
                            </Rectangle>

                            <ContentPresenter x:Name="ButtonContent" VerticalAlignment="Center">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="./#Cinzel"/>
                                        <Setter Property="FontSize" Value="38"/>
                                        <Setter Property="Foreground" Value="#BFA472"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </StackPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                            <ColorAnimation Storyboard.TargetName="ButtonContent" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" To="#D4AF37" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Grid.Resources>

    <Grid.Background>
        <RadialGradientBrush Center="0.5,0.4" GradientOrigin="0.5,0.4" RadiusX="0.8" RadiusY="0.8">
            <GradientStop Color="#300A52" Offset="0.0" />
            <GradientStop Color="#050208" Offset="1.0" />
        </RadialGradientBrush>
    </Grid.Background>

    <Grid Margin="100,60">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock x:Name="LogoAura"
                       Text="KINGSCRAFT"
                       FontSize="112"
                       Foreground="#D4AF37"
                       FontFamily="./#Cinzel"
                       Opacity="0.4"
                       RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <ScaleTransform ScaleX="1.06" ScaleY="1.06"/>
                </TextBlock.RenderTransform>
                <TextBlock.Effect>
                    <BlurEffect Radius="65"/>
                </TextBlock.Effect>
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="LogoAura"
                                                 Storyboard.TargetProperty="Opacity"
                                                 From="0.2" To="0.6"
                                                 Duration="0:0:4"
                                                 AutoReverse="True"
                                                 RepeatBehavior="Forever">
                                    <DoubleAnimation.EasingFunction>
                                        <SineEase EasingMode="EaseInOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>

            <TextBlock x:Name="LogoPulse"
                       Text="KINGSCRAFT"
                       FontSize="110"
                       Foreground="#D4AF37"
                       FontFamily="./#Cinzel"
                       Opacity="0.2">
                <TextBlock.Effect>
                    <BlurEffect Radius="25"/>
                </TextBlock.Effect>
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="LogoPulse"
                                                 Storyboard.TargetProperty="Opacity"
                                                 From="0.1" To="0.7"
                                                 Duration="0:0:3"
                                                 AutoReverse="True"
                                                 RepeatBehavior="Forever">
                                    <DoubleAnimation.EasingFunction>
                                        <SineEase EasingMode="EaseInOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="KINGSCRAFT"
                           FontSize="110"
                           Foreground="#D4AF37"
                           FontFamily="./#Cinzel">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="10" Color="#D4AF37" ShadowDepth="0" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="THE SANDBOX WITHOUT LIMITS"
                           FontSize="18"
                           Foreground="#BFA472"
                           FontFamily="./#Cinzel"
                           HorizontalAlignment="Center"
                           Opacity="0.6"
                           Margin="0,-15,0,0"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Left">
            <Button Content="ENTER WORLD" Style="{StaticResource MenuButtonStyle}"/>
            <Button Content="OPTIONS" Style="{StaticResource MenuButtonStyle}"/>
            <Button x:Name="QuitButton" Content="QUIT GAME" Style="{StaticResource MenuButtonStyle}"/>
        </StackPanel>
    </Grid>

    <Rectangle IsHitTestVisible="False">
        <Rectangle.Fill>
            <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.5,0.5" RadiusX="1.1" RadiusY="1.1">
                <GradientStop Color="#00000000" Offset="0.4" />
                <GradientStop Color="#FF050208" Offset="1.1" />
            </RadialGradientBrush>
        </Rectangle.Fill>
    </Rectangle>

</Grid>